# Optional: Mapbox GL (omit to use SVG fallback heatmap)
VITE_MAPBOX_ACCESS_TOKEN=

# Optional: backend base URL — leave unset in dev to use Vite proxy to http://127.0.0.1:5000/api
# VITE_API_URL=http://127.0.0.1:5000
